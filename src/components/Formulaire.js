import React, { Component } from 'react'

export default class Formulaire extends Component {
    render() {
        return (
            <form>
                Formulaire !
            </form>
        )
    }
}
